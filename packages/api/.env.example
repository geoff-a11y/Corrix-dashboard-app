# Corrix API - Environment Variables
# Copy this to .env for local development

# Server
PORT=3001
NODE_ENV=development

# Database (PostgreSQL)
# For Supabase: Use the connection string from Project Settings > Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/corrix_dashboard

# JWT Secret (generate a secure random string for production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Redis (optional - for caching)
# Leave empty to disable Redis caching
REDIS_URL=

# CORS Origins (comma-separated)
# Add your frontend URLs here
CORS_ORIGINS=http://localhost:3000,http://localhost:3002,http://localhost:5173

# Supabase (for extension sync - optional)
SUPABASE_URL=
SUPABASE_ANON_KEY=
